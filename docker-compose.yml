services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: snipr-backend
    ports:
      - '3000:3000'
    volumes:
      - ./backend:/app
      - /app/node_modules
    env_file:
      - .env
